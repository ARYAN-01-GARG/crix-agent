{
  "name": "@crix/events",
  "version": "0.1.0",
  "private": true,
  "description": "Event emitter abstraction for crix — Node EventEmitter locally, Redis Pub/Sub in server mode",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crix/cache": "workspace:*",
    "@crix/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.7.3"
  }
}
